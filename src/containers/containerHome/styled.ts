import styled from 'styled-components'

export const CtnHome = styled.div`
    background-color: #000;
    display: grid;
    grid-template-columns:  220px 1fr;
    height: 100vh;
    overflow: hidden;
`

export const SideMain = styled.aside`
    width: 100%;
    height: 100%;
    overflow-y:  auto;
`

export const Main = styled.main`
    padding: 32px;
    overflow-y: auto;
`